// Creative Touch — runtime config
// This is the ONLY file you need to edit when deploying.
//
// Set this to the public address where your API Gateway is reachable.
// - Local testing:      http://localhost:8080
// - Deployed on a VPS:  http://YOUR_SERVER_IP:8080  (or https://api.yourdomain.com)
window.CT_API_BASE = "http://localhost:8080";
